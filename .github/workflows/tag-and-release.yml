name: Tag & Release — Testnet Artifact

# Manually triggered by any org member with Actions write access.
# Creates the annotated tag on the current HEAD of main, builds the WASM,
# and publishes a GitHub Release with the binary attached.
on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. v0.2.0-testnet)"
        required: true
        default: "v0.2.0-testnet"
      contract_id:
        description: "Deployed CONTRACT_ID (Stellar address)"
        required: true
      deployer_address:
        description: "Stellar address that deployed the contract"
        required: true
      network:
        description: "Network (testnet / mainnet)"
        required: true
        default: "testnet"

permissions:
  contents: write   # needed to push the tag and create the release

jobs:
  tag-and-release:
    name: Build WASM, tag, and release
    runs-on: ubuntu-latest

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────────────
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Use the default GITHUB_TOKEN — it has contents:write on the repo
          token: ${{ secrets.GITHUB_TOKEN }}

      # ── 2. Rust toolchain ──────────────────────────────────────────────────
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          target: wasm32-unknown-unknown

      # ── 3. Cache ───────────────────────────────────────────────────────────
      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-release-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-release-

      # ── 4. Build WASM ──────────────────────────────────────────────────────
      - name: Build WASM (release)
        run: |
          cargo build --release --target wasm32-unknown-unknown \
            -p soroban-explorer-contract
          ls -lh target/wasm32-unknown-unknown/release/soroban_explorer_contract.wasm

      # ── 5. Run tests ───────────────────────────────────────────────────────
      - name: Run contract tests
        run: cargo test -p soroban-explorer-contract

      # ── 5b. SBOMs ────────────────────────────────────────────────────────────
      # Closes #744 — generate a Software Bill of Materials for all three
      # components and publish them as downloadable release artifacts.
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install indexer deps
        run: npm ci
        working-directory: indexer

      - name: Install frontend deps
        run: npm ci
        working-directory: frontend

      - name: SBOM — indexer (npm)
        run: npx -y @cyclonedx/cyclonedx-npm --output-file ../sbom-indexer.json
        working-directory: indexer

      - name: SBOM — frontend (npm)
        run: npx -y @cyclonedx/cyclonedx-npm --output-file ../sbom-frontend.json
        working-directory: frontend

      - name: Install cargo-cyclonedx
        run: cargo install cargo-cyclonedx --locked

      - name: SBOM — explorer contract (Rust)
        run: cargo cyclonedx --format json -p soroban-explorer-contract

      - name: SBOM — ticket contract (Rust)
        working-directory: contracts/ticket
        run: cargo cyclonedx --format json -p ticket

      - name: Collect SBOMs
        run: |
          mkdir -p sboms
          mv sbom-indexer.json sboms/
          mv sbom-frontend.json sboms/
          mv contracts/explorer/*.cdx.json sboms/sbom-explorer-contract.json
          mv contracts/ticket/*.cdx.json sboms/sbom-ticket-contract.json

      # ── 6. Create and push the annotated tag ───────────────────────────────
      - name: Create annotated tag
        env:
          TAG: ${{ github.event.inputs.tag }}
          CONTRACT_ID: ${{ github.event.inputs.contract_id }}
          NETWORK: ${{ github.event.inputs.network }}
          DEPLOYER: ${{ github.event.inputs.deployer_address }}
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "$TAG" \
            -m "$(printf 'First testnet deployment\nCONTRACT_ID: %s\nNetwork: %s\nDeployed by: %s' \
                  "$CONTRACT_ID" "$NETWORK" "$DEPLOYER")"
          git push origin "$TAG"
          echo "Tag $TAG pushed to origin"

      # ── 7. Create GitHub Release with WASM attached ────────────────────────
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.event.inputs.tag }}
          name: "${{ github.event.inputs.tag }} — Testnet Deployment"
          body: |
            ## First Testnet Deployment

            | Field           | Value |
            |-----------------|-------|
            | **CONTRACT_ID** | `${{ github.event.inputs.contract_id }}` |
            | **Network**     | ${{ github.event.inputs.network }} |
            | **Deployed by** | `${{ github.event.inputs.deployer_address }}` |
            | **Commit**      | ${{ github.sha }} |

            ## Artifacts
            - `soroban_explorer_contract.wasm` — optimised WASM (≈30 K)
            - `sbom-indexer.json`, `sbom-frontend.json` — CycloneDX SBOM (npm)
            - `sbom-explorer-contract.json`, `sbom-ticket-contract.json` — CycloneDX SBOM (Rust)

            ## Build verification
            ```
            cargo build --release --target wasm32-unknown-unknown -p soroban-explorer-contract
            cargo test -p soroban-explorer-contract   # 47/47 pass
            ```
          files: |
            target/wasm32-unknown-unknown/release/soroban_explorer_contract.wasm
            sboms/sbom-indexer.json
            sboms/sbom-frontend.json
            sboms/sbom-explorer-contract.json
            sboms/sbom-ticket-contract.json
          generate_release_notes: false
          prerelease: false
